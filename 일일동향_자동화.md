# 일일 동향 브리프 자동화 안내 (차은수 trendwatch v2)

매일 아침 차은수(`trendwatch`)가 본 과제 관련 최신자료를 **논문·기사·보고서·정책동향** 4분류
브리프(총 10건 이내, 중복 최소화, 연관성·시사점 포함)로 생성하고, 연구자가 고른 자료를
LLM-Wiki(서지원) 또는 내부문헌(윤서지)으로 편입할 수 있게 하는 자동화 구성이다.

## 동작 개요
```
[스케줄러] → scripts/trendwatch_daily.(bat|sh)
   → claude -p "…서달미가 trendwatch 일일 브리프 모드 1회 실행…"
   → workspace/trend/daily_brief_<날짜>.md 생성 + #general·#trendwatch 보고
   → (연구자 선택) 편입 후보 keep → 서달미가 paperwiki/litreview로 라우팅
```

## 사전 조건
- Claude Code CLI 설치 및 로그인 (`claude` 명령이 PATH에 있어야 함)
- 프로젝트 루트에서 최소 1회 정상 실행 확인 (`.env`의 `$KOTI_DESKTOP_DIR` 등 경로 설정)
- (선택) 메신저 실행 중이어야 `#trendwatch` 채널에 보고가 남음 — 꺼져 있어도 브리프 파일은 생성됨

## Windows — 작업 스케줄러 등록
1. `작업 스케줄러` 실행 → **기본 작업 만들기**
2. 이름: `KOTI 일일 동향` · 트리거: **매일**, 시간 예) 오전 8:30
3. 동작: **프로그램 시작** → 프로그램/스크립트:
   `D:\김예원\1.탄소\멀티에이전트\과제 멀티 에이전트\scripts\trendwatch_daily.bat`
   시작 위치: `D:\김예원\1.탄소\멀티에이전트\과제 멀티 에이전트`
4. 마침. (테스트: 해당 작업 우클릭 → **실행**)
- 로그: `workspace\trend\logs\daily_<날짜>.log`

## macOS/Linux — cron 등록
```bash
chmod +x "scripts/trendwatch_daily.sh"
crontab -e
# 매일 08:30 실행
30 8 * * * "/경로/과제 멀티 에이전트/scripts/trendwatch_daily.sh"
```

## 산출물 서식 (daily_brief_<날짜>.md)
```
## 오늘의 헤드라인 3줄
## 논문        (0건이면 "금일 특이동향 없음")
## 기사
## 보고서
## 정책동향
## 편입 후보 체크리스트   ← 연구자가 keep 표시 → 위키/내부문헌 편입
```
각 항목: 출처·발표일 / 요지 / **본 사업 연관성** / **시사점**. 상한 10건, 무근거 항목 금지.

## 편입 워크플로 (선택 자료 → 지식 축적)
1. 브리프 하단 체크리스트에서 연구자가 keep 표시
2. 서달미가 확인 후 라우팅
   - 외부 논문·학술자료 → `paperwiki`(서지원) ingest → LLM-Wiki 축적
   - 기관 보고서·내부성 문서 → `litreview`(윤서지) 내부문헌 편입
3. 이후 ⑩논문작성(한지음)이 이 근거를 그대로 인용

## 주의
- `--dangerously-skip-permissions` 는 무인 실행을 위한 옵션이다. 사내 정책상 부담되면
  이 플래그를 빼고 대화형으로 실행하거나, 별도 승인 설정을 검토한다.
- 억지로 자료를 찾지 않는 것이 설계 원칙이다. "특이동향 없음"도 정상 결과다.
